simple Source
{
    parameters:
        volatile double interArrival1 @unit(s);
        volatile double interArrival2 @unit(s);
        volatile double interArrival3 @unit(s);

    gates:
        output out[];
}
